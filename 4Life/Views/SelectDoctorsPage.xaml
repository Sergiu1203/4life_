<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodel="clr-namespace:_4Life.ViewModels"
             x:Class="_4Life.Views.SelectDoctorsPage"
             x:DataType="viewmodel:SelectDoctorsViewModel"
             Title="Choose Your Doctors"
             BackgroundColor="White">

    <Grid RowDefinitions="*, Auto" Padding="20">

        <CollectionView Grid.Row="0" ItemsSource="{Binding Doctors}">
            <CollectionView.EmptyView>
                <Label Text="No doctors registered yet." HorizontalOptions="Center"
                       TextColor="Gray" Margin="20"/>
            </CollectionView.EmptyView>
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="viewmodel:DoctorSelectionItem">
                    <Frame Margin="0,6" Padding="15" CornerRadius="12" HasShadow="True">
                        <Grid ColumnDefinitions="*, Auto">
                            <VerticalStackLayout Grid.Column="0" Spacing="2">
                                <Label Text="{Binding Doctor.FullName}"
                                       FontAttributes="Bold" FontSize="16"/>
                                <Label Text="{Binding Doctor.Specialization}"
                                       FontSize="13" TextColor="Gray"/>
                                <Label Text="{Binding Doctor.MedicalId, StringFormat='ID: {0}'}"
                                       FontSize="11" TextColor="Gray"/>
                            </VerticalStackLayout>

                            <CheckBox Grid.Column="1"
                                      IsChecked="{Binding IsSelected}"
                                      Color="{StaticResource Primary}"
                                      VerticalOptions="Center"/>
                        </Grid>
                    </Frame>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

        <Button Grid.Row="1"
                Text="Save Selection"
                Command="{Binding SaveSelectionCommand}"
                BackgroundColor="{StaticResource Primary}"
                TextColor="White"
                CornerRadius="20"
                HeightRequest="50"
                Margin="0,10,0,0"/>
    </Grid>
</ContentPage>
