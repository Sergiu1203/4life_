<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="_4Life.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:_4Life"
    xmlns:views="clr-namespace:_4Life.Views"
    Title="_4Life"
    
    Shell.BackgroundColor="{StaticResource Primary}"
    Shell.ForegroundColor="White"
    Shell.TitleColor="White"
    Shell.UnselectedColor="#B3FFFFFF">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate views:LoginPage}"
        Route="LoginPage" />
    
    <ShellContent
        Title="Register"
        ContentTemplate="{DataTemplate views:RegisterPage}"
        Route="RegisterPage" 
        />
    <ShellContent
    Title="Dashboard"
    ContentTemplate="{DataTemplate views:DashboardPage}"
    Route="DashboardPage" />

    <ShellContent
        Title="Doctor Portal"
        ContentTemplate="{DataTemplate views:DoctorDashboardPage}"
        Route="DoctorDashboardPage" />

</Shell>
